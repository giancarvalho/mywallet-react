import styled from "styled-components";

const Title = styled.h1`
    display: inline-block;
    font-size: 24px;
    font-weight: 700;
    color: #fff;
    margin-bottom: 20px;
`;

export default Title;
