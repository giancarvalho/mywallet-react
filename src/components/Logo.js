import styled from "styled-components";

export default function Logo() {
    return <LogoContainer>MyWallet</LogoContainer>;
}

const LogoContainer = styled.h1`
    font-size: 28px;
    font-family: "Saira Stencil One", cursive;
    color: #fff;
    margin-bottom: 20px;
`;
